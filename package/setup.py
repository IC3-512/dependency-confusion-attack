from setuptools import setup, find_packages

setup(
    name='package_346234293',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Any dependencies your package needs
    ]
)
